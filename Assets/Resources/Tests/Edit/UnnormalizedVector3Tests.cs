using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

/*public class UnnormalizedVector3Tests
{


    [Test]
    public void ExplicitConversionToVector3Works()
    {
        // Arrange
        UnnormalizedVector3 unnormalizedVector = new(1, 2, 3);

        // Act
        Vector3 vector = (Vector3)unnormalizedVector;

        // Assert
        Assert.AreEqual(unnormalizedVector.Vector, vector);
    }

    [Test]
    public void ConstructorWithFloatsProducesUnnormalizedVector()
    {
        // Arrange
        UnnormalizedVector3 unnormalizedVector = new(1, 2, 3);

        // Act

        // Assert
        Assert.AreEqual(new Vector3(1, 2, 3), unnormalizedVector.Vector);
    }

    [Test]
    public void ConstructorWithVector3ProducesUnnormalizedVector()
    {
        // Arrange
        Vector3 testVector = new(1, 2, 3);

        // Act
        UnnormalizedVector3 unnormalizedVector = new(testVector);

        // Assert
        Assert.AreEqual(testVector, unnormalizedVector.Vector);
    }

    
}
*/